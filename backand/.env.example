# Environment variables declared in this file are automatically made available to Prisma.
# See the documentation for more detail: https://pris.ly/d/prisma-schema#accessing-environment-variables-from-the-schema

# Prisma supports the native connection string format for PostgreSQL, MySQL, SQLite, SQL Server, MongoDB and CockroachDB.
# See the documentation for all the connection string options: https://pris.ly/d/connection-strings

DATABASE_USER=johndoe
DATABASE_PASSWORD=randompassword
DATABASE_NAME=mydb
DATABASE_PORT=5432
DATABASE_URL=postgresql://${DATABASE_USER}:${DATABASE_PASSWORD}@localhost:${DATABASE_PORT}/${DATABASE_NAME}

DONOR_DATABASE_USER=info
DONOR_DATABASE_PASSWORD=root
DONOR_DATABASE_NAME=donor
DONOR_DATABASE_PORT=5433
DONOR_DATABASE_URL=postgresql://${DONOR_DATABASE_USER}:${DONOR_DATABASE_PASSWORD}@localhost:${DONOR_DATABASE_PORT}/${DONOR_DATABASE_NAME}

MAIL_PASSWORD=MAIL_PASSWORD
MAIL_USER=MAIL_USER

JWT_SECRET_KEY=JWT_SECRET_KEY

TG_BOT_KEY=TG_BOT_KEY